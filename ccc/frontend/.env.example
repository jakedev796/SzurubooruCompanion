# Optional: API base URL at build time (e.g. https://api.ccc.example.com or http://host:21425).
# Leave unset for: localhost (uses http://localhost:21425/api) or same-origin /api behind a reverse proxy.
# VITE_API_BASE=
